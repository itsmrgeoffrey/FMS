.env
bank_config.yaml
__pycache__/
*.pyc
*.pyo
.venv/
venv/
fms.db
node_modules/
.next/
*.log
*.log.*
logs/

# Local tooling / editor config
.claude/
.vscode/
.idea/

# Downloaded OFAC list data (refreshed via scripts/update_ofac.py; sample stays committed)
data/ofac_sdn.json
data/ofac_consolidated.json
data/sdn.csv
data/alt.csv
data/cons_prim.csv
data/cons_alt.csv
# Institution-supplied screening lists (may contain internal watch-list data)
data/extra_lists/

# Private production environment — real credentials, NEVER committed
/prod/

# The demo/test environment IS committed (no real secrets). Re-include its
# bank_config.yaml, which the global bank_config.yaml rule above would ignore.
!test/bank_config.yaml

# Maintainer-only internal architecture notes (kept local, not published)
docs/CODE_TOUR.md
